NEXT_PUBLIC_API_BASE=/api
# Internal backend URL used by the Next.js server-side proxy.
# Keep this as 127.0.0.1 when frontend and backend run on the same host.
BACKEND_INTERNAL_BASE=http://127.0.0.1:8000
NEXT_PUBLIC_DEFAULT_JIRA_DOMAIN=
